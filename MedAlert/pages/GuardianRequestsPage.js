import { View, StyleSheet, Text } from "react-native";

export default function GuardianRequestsPage() {
  return (
    <View>
      <Text>Guardian Requests Page</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flexDirection: "column",
    alignItems: "center",
    backgroundColor: "#fff",
    flex: 1,
    width: "100%",
  },
});
